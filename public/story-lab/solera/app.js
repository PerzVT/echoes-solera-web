import {scenes,byId,validScene} from './scenes.js?v=polish-1';
import {clips,audio as audioFiles} from './media.js?v=polish-1';
import {mediaAssets} from './assets.generated.js?v=polish-1';
import {preloadImage,preloadVideoMetadata,waitForVideo} from './media-loader.js?v=polish-1';
const $=id=>document.getElementById(id),video=$('video'),voice=new Audio(),reduced=matchMedia('(prefers-reduced-motion:reduce)');
const spatialIds=new Set(['selection','classes','training','lobby']);
let current=validScene(location.hash.slice(1)),history=[],epoch=0,controller,timer,confirmTimer,loaderTimer,auto=false,sound=false,confirming=false,activeVideo=false,phase='loading',firstPaint=true,inputMode='pointer',resumeVisible=false;
const pause=ms=>new Promise(resolve=>setTimeout(resolve,ms));
const imageURL=name=>mediaAssets[name]||'assets/'+name;
const mediaURL=src=>mediaAssets[src]||src;
const portrait=()=>document.body.classList.contains('portrait')||matchMedia('(max-width:700px)').matches;
scenes.forEach(s=>{const o=document.createElement('option');o.value=s.id;o.textContent=s.chapter.split(' / ')[0]+' · '+s.title;$('sceneSelect').append(o);});
document.addEventListener('pointerdown',()=>inputMode='pointer');document.addEventListener('keydown',()=>inputMode='keyboard');
function stop(){controller?.abort();clearTimeout(timer);clearTimeout(confirmTimer);clearTimeout(loaderTimer);video.onended=video.onerror=video.onwaiting=video.onplaying=null;voice.onended=voice.onerror=null;video.pause();voice.pause();video.removeAttribute('src');video.load();video.hidden=true;voice.removeAttribute('src');confirming=false;activeVideo=false;$('buffering').hidden=true;}
function syncControls(){const loaded=phase!=='loading'&&phase!=='error';$('tour').textContent=auto?'Pause walkthrough':'Play walkthrough';$('tour').setAttribute('aria-pressed',String(auto));$('audio').textContent=sound?'Sound on':'Sound off';$('audio').setAttribute('aria-pressed',String(sound));$('soundCue').hidden=!activeVideo||sound||phase==='ended'||!loaded;$('sceneBack').disabled=$('back').disabled=!history.length;$('sceneReplay').disabled=!loaded;$('clipPlay').hidden=!activeVideo;$('clipPlay').textContent=phase==='playing'?'Pause scene':'Play scene';$('clipSkip').hidden=!activeVideo||phase==='ended';}
function beginLoading(first){$('app').dataset.ready='false';$('stage').setAttribute('aria-busy','true');$('stage').inert=true;$('loadError').hidden=true;$('loader').dataset.mode=first?'initial':'scene';$('loader').dataset.state='loading';$('loader').style.setProperty('--load-progress','.12');$('loadingText').textContent=first?'Loading Solera':'Loading scene';if(first)$('loader').hidden=false;else{$('loader').hidden=true;loaderTimer=setTimeout(()=>{$('loader').hidden=false;},120);}}
async function reveal(token,start,first){if(first&&!reduced.matches&&inputMode!=='keyboard')await pause(Math.max(0,1100-(performance.now()-start)));if(token!==epoch)return false;clearTimeout(loaderTimer);$('loader').style.setProperty('--load-progress','1');if(!$('loader').hidden){$('loader').dataset.state='exiting';await pause(reduced.matches?0:first?500:180);}if(token!==epoch)return false;$('loader').hidden=true;$('app').dataset.animate=String(!reduced.matches&&inputMode!=='keyboard');$('app').dataset.ready='true';$('stage').setAttribute('aria-busy','false');$('stage').inert=false;firstPaint=false;return true;}
function go(id,{back=false}={}){if(!byId.has(id))return;if(!back&&id!==current)history.push(current);current=id;window.history.replaceState(null,'','#'+id);$('gameMenu').open=false;render();}
function nextTarget(){const s=byId.get(current);return activeVideo&&s.videoNext?s.videoNext:s.next;}
function advance(){if(phase==='loading')return;const s=byId.get(current);if(s.end){auto=false;syncControls();return;}s.choices?choose(s.choices.findIndex(c=>!c.locked)):go(nextTarget());}
function choose(i){if(confirming||phase==='loading')return;const s=byId.get(current),c=s.choices?.[i];if(!c)return;if(c.locked){$('feedback').textContent=c.label+' is unavailable in this demo.';return;}if(s.id==='selection'){confirming=true;clearTimeout(timer);$('stage').classList.add('nova-lit');document.querySelector('.world-target[data-index="1"]')?.classList.add('selected');confirmTimer=setTimeout(()=>go(c.to),inputMode==='keyboard'||reduced.matches?0:280);}else go(c.to);}
function schedule(){clearTimeout(timer);const s=byId.get(current);if(!auto||phase==='loading'||phase==='playing'||phase==='paused')return;if(s.end){auto=false;syncControls();return;}if(voice.getAttribute('src')&&!voice.ended)return;const token=epoch;timer=setTimeout(()=>{if(token===epoch)advance();},s.seconds*1000);}
function renderActions(s){$('choices').replaceChildren();const options=s.choices||[{label:s.end?'Play again':activeVideo&&s.videoAction?s.videoAction:s.action||'Continue'}];options.forEach((c,i)=>{const b=document.createElement('button');b.textContent=(c.locked?'⊘ ':'')+c.label;b.className=c.locked?'':'primary';if(c.locked)b.setAttribute('aria-disabled','true');b.onclick=()=>{if(s.choices)choose(i);else if(s.end){auto=false;history=[];go('selection',{back:true});}else go(nextTarget());};$('choices').append(b);});}
function novaLight(on){$('stage').classList.toggle('nova-lit',on);if(on&&clips.spawn?.landscape)preloadVideoMetadata(mediaURL(clips.spawn.landscape));}
function worldChoices(s){const spatial=spatialIds.has(s.id);$('stage').classList.toggle('spatial',spatial);$('stage').classList.toggle('selection',s.id==='selection');$('stage').dataset.scene=s.id;$('hotspots').hidden=!spatial;$('hotspots').setAttribute('aria-label',s.id==='selection'?'Character selection':'World choices');$('hotspots').replaceChildren();$('hoverImage').hidden=s.id!=='selection';$('stage').classList.remove('nova-lit');if(!spatial)return;
 const positions={selection:[[14,4,23,91],[38,4,24,91],[65,4,23,91]],classes:[[3,10,24,44],[37,8,26,46],[69,10,25,44]],training:[[25,57,58,36]],lobby:[[58,8,20,85],[74,51,19,44]]}[s.id];
 s.choices.forEach((c,i)=>{const b=document.createElement('button');b.className='hotspot world-target';b.dataset.index=String(i);const [x,y,w,h]=positions[i];Object.assign(b.style,{left:x+'%',top:y+'%',width:w+'%',height:h+'%'});b.setAttribute('aria-label',c.label+(c.locked?', locked':''));if(c.locked)b.setAttribute('aria-disabled','true');const label=document.createElement('span');label.className='world-label';const glyph=document.createElement('span');glyph.className=c.locked?'world-glyph locked-glyph':'world-glyph';glyph.textContent=c.locked?'⊘':s.id==='classes'?'↑':'◇';glyph.setAttribute('aria-hidden','true');const name=document.createElement('span');name.textContent=s.id==='selection'?['Ya','Nova','Or-en'][i]:c.label;label.append(glyph,name);b.append(label);if(s.id==='selection'&&i===1){b.onpointerenter=()=>novaLight(true);b.onpointerleave=()=>{if(!confirming&&document.activeElement!==b)novaLight(false);};b.onfocus=()=>novaLight(true);b.onblur=()=>{if(!confirming)novaLight(false);};}b.onclick=()=>choose(i);$('hotspots').append(b);});}
function reflow(){const p=document.querySelector('.picture').getBoundingClientRect(),stage=$('stage').getBoundingClientRect(),space=stage.bottom-p.bottom-64,compact=(stage.width<1100||document.body.classList.contains('portrait'))&&space>90;$('stage').classList.toggle('compact',compact);(compact?$('letterboxActions'):$('imageActions')).append($('choices'));(compact?$('letterboxDetails'):$('identityHome')).append($('identity'));$('letterboxDock').hidden=!compact;$('letterboxDock').style.top=(p.bottom-stage.top+14)+'px';$('letterboxDock').style.maxHeight=Math.max(60,space)+'px';$('hotspots').setAttribute('aria-hidden',String(compact));$('hotspots').querySelectorAll('button').forEach(b=>b.tabIndex=compact?-1:0);}
new ResizeObserver(reflow).observe(document.querySelector('.picture'));window.addEventListener('resize',reflow);
function warmNext(s){if(navigator.connection?.saveData||/2g/.test(navigator.connection?.effectiveType||''))return;const ids=s.choices?s.choices.filter(c=>!c.locked).map(c=>c.to):[s.videoNext||s.next];for(const id of ids){const n=byId.get(id);if(n)preloadImage(imageURL(n.image)).catch(()=>{});}if(s.id==='selection'&&clips.spawn?.landscape)preloadVideoMetadata(mediaURL(clips.spawn.landscape));}
function showPlayButton(){const b=document.createElement('button');b.textContent='▶ Play intro';b.className='primary';b.onclick=()=>playVideo();$('choices').replaceChildren(b);$('choices').hidden=false;$('stage').classList.remove('clip-playing');}
function playVideo(){const token=epoch;if(!activeVideo)return;clearTimeout(timer);phase='playing';$('stage').classList.add('clip-playing');$('choices').hidden=true;syncControls();video.play().then(()=>{if(token===epoch)syncControls();}).catch(()=>{if(token!==epoch)return;phase='paused';showPlayButton();syncControls();});}
function finishVideo(){phase='ended';$('stage').classList.remove('clip-playing');$('buffering').hidden=true;renderActions(byId.get(current));$('choices').hidden=false;syncControls();if(auto)advance();}
function videoFallback(){activeVideo=false;phase='ready';video.hidden=true;$('stage').classList.remove('cinematic','portrait-video','clip-playing');$('buffering').hidden=true;$('mediaStatus').textContent='Comic preview';$('feedback').textContent='Animation unavailable. Replay to retry, or continue with the comic.';renderActions(byId.get(current));$('choices').hidden=false;reflow();syncControls();schedule();}
async function render(){stop();const token=++epoch,start=performance.now(),first=firstPaint,s=byId.get(current);controller=new AbortController();const signal=controller.signal;phase='loading';beginLoading(first);syncControls();
 try{const needed=[preloadImage(imageURL(s.image))];if(s.id==='selection')needed.push(preloadImage(imageURL('selection-highlight.png')));await Promise.all(needed);if(token!==epoch)return;$('loader').style.setProperty('--load-progress','.65');$('poster').src=imageURL(s.image);$('poster').alt=s.alt;await $('poster').decode().catch(()=>{});if(token!==epoch)return;$('hoverImage').src=imageURL('selection-highlight.png');$('stage').style.setProperty('--scene-bg',`url("${imageURL(s.image)}")`);$('chapter').textContent=s.chapter;$('title').textContent=s.title;$('speaker').textContent=s.speaker||'';$('caption').textContent=s.caption;$('feedback').textContent='';$('dialog').classList.toggle('has-copy',Boolean(s.caption&&!spatialIds.has(s.id)));$('guideArrow').hidden=!s.arrow;$('identity').hidden=!s.identity;$('sceneSelect').value=s.id;$('counter').textContent=(scenes.indexOf(s)+1)+' / '+scenes.length;worldChoices(s);
 const clip=clips[s.clip],portraitSource=portrait()&&clip?.portrait,src=clip&&(portraitSource||clip.landscape);let videoFailed=false;if(src){try{video.poster=imageURL(s.image);video.muted=!sound;await waitForVideo(video,mediaURL(src),{signal});activeVideo=true;}catch(error){if(token!==epoch||error.name==='AbortError')return;videoFailed=true;activeVideo=false;}}if(token!==epoch)return;
 video.hidden=!activeVideo;$('stage').classList.toggle('portrait-video',Boolean(activeVideo&&portraitSource));$('stage').classList.toggle('cinematic',Boolean(activeVideo&&s.cinematic));$('stage').classList.remove('clip-playing');$('mediaStatus').textContent=activeVideo?'Video preview':'Comic preview';renderActions(s);$('choices').hidden=activeVideo;reflow();if(!await reveal(token,start,first))return;phase='ready';if(videoFailed)$('feedback').textContent='Animation unavailable. Replay to retry, or continue with the comic.';
 if(activeVideo){video.onended=()=>{if(token===epoch)finishVideo();};video.onerror=()=>{if(token===epoch)videoFallback();};video.onwaiting=()=>{if(token===epoch&&phase==='playing')$('buffering').hidden=false;};video.onplaying=()=>{if(token===epoch)$('buffering').hidden=true;};playVideo();}
 const audioSrc=s.audio&&audioFiles[s.audio];if(audioSrc){voice.src=audioSrc;voice.muted=!sound;voice.onended=()=>{if(token===epoch)schedule();};voice.onerror=()=>{if(token===epoch){voice.removeAttribute('src');schedule();}};voice.play().catch(()=>{if(token===epoch){voice.removeAttribute('src');schedule();}});}warmNext(s);syncControls();schedule();
 }catch(error){if(token!==epoch||error.name==='AbortError')return;clearTimeout(loaderTimer);phase='error';$('loader').hidden=false;$('loader').dataset.state='error';$('loadingText').textContent='Unable to load scene';$('loadErrorText').textContent='This scene could not load. Check your connection and try again.';$('loadError').hidden=false;syncControls();}}
function back(){const id=history.pop();if(id){auto=false;go(id,{back:true});}}
function replay(){auto=false;render();}
$('sceneBack').onclick=$('back').onclick=back;$('sceneReplay').onclick=$('retryLoad').onclick=replay;
$('tour').onclick=()=>{auto=!auto;if(auto){if(activeVideo&&phase!=='ended')playVideo();else schedule();}else{clearTimeout(timer);clearTimeout(confirmTimer);confirming=false;if(activeVideo&&!video.ended){video.pause();phase='paused';}voice.pause();}syncControls();};
$('audio').onclick=()=>{sound=!sound;video.muted=!sound;voice.muted=!sound;syncControls();};$('soundCue').onclick=()=>{sound=true;video.muted=false;voice.muted=false;playVideo();};
$('restart').onclick=()=>{auto=false;history=[];go('selection',{back:true});};$('clipPlay').onclick=()=>{if(phase==='playing'){video.pause();phase='paused';syncControls();}else{if(video.ended)video.currentTime=0;playVideo();}};
$('clipSkip').onclick=()=>{video.pause();auto=false;finishVideo();};$('sceneSelect').onchange=e=>{auto=false;go(e.target.value);};
$('format').onclick=()=>{document.body.classList.toggle('portrait');const p=document.body.classList.contains('portrait');$('format').textContent=p?'16:9':'9:16';$('format').setAttribute('aria-label',p?'Switch to landscape preview':'Switch to portrait preview');render();};
window.addEventListener('hashchange',()=>{const id=validScene(location.hash.slice(1));if(id!==current){auto=false;history.push(current);current=id;render();}});
document.addEventListener('visibilitychange',()=>{if(document.hidden){clearTimeout(timer);resumeVisible=activeVideo&&phase==='playing';video.pause();voice.pause();}else if(resumeVisible){resumeVisible=false;playVideo();}else if(auto)schedule();});
document.addEventListener('keydown',e=>{if(e.key==='Escape')$('gameMenu').open=!$('gameMenu').open;});
render();

